allprojects {
    group GROUP
    version VERSION

    apply plugin: 'java'
    apply plugin: 'idea'
    apply plugin: 'maven'

    // JVM 版本号要求
    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenLocal()

        maven { url 'http://rddgit.changhong.com:8081/nexus/content/groups/ECMP_Group' }
    }

/* 冲突失败策略设置*/
//    configurations.all {
//        resolutionStrategy { failOnVersionConflict() }
//    }
/* 强制指定版本策略设置*/
//    dependencies {
//        compile group:'org.hibernate.javax.persistence',name:'hibernate-jpa-2.1-api',version:'1.0.0.Final',force:true
//    }

//    // 依赖
//    dependencies {
//
//        compile(
//                "org.hibernate.javax.persistence:hibernate-jpa-2.1-api:1.0.0.Final"
//        )
//    }
}